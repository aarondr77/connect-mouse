# Automated tests

Runs a one-shot checklist against the LabJack (joystick rails, motion, ultrasonic range, enable line off).

**Before running:** Stop the script on the **Live** tab — only one process should use the LabJack at a time.

Click **Run Workflow** to start. Each test shows pass/fail in the table below.
